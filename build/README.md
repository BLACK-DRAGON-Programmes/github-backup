# build

Compiled binaries and build output for the github-backup project.

## Contents

- `backup.exe` — Final compiled binary (not committed to repo, built locally)

## Notes

This directory holds build artifacts only. No source code or build scripts live here. The `.gitkeep` file ensures the directory is tracked by git even when empty.
